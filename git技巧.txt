git init #创建git项目

git remote add origin ***  #将git与github关联

git remote rm origin  #删除与远程厂库的连接

git remote -v #查看关联远程厂库

git add .  #添加到暂存区

git commit -m "第二次更新"  #添加到本地仓库

git status  #刷新暂存区状态


git push -u origin **** #将本地仓库提交并且与远程仓库关联 

git clone url  

git branch #列出分支

git branch -a #查看本地和远程仓库

git branch ***  #创建分支

git branch -vv #查看本地仓库和远程仓库的关联情况

git pull origin master --allow-unrelated-histories  #允许合并不相关内容

git ls-files  #查看本地仓库的文件

git remote add origin git@github.com:flora0103/example.git #将git与远程仓库链接


